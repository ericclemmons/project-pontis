{
  "private": true,
  "name": "project-pontis",
  "version": "1.0.0",
  "scripts": {
    "prepare": "pnpm --filter \"./packages/**\" run build",
    "dev": "pnpm --if-present --recursive --stream run dev"
  }
}
